---
title: Installation
---

# Installation

`@vincle/core` has zero runtime dependencies. Install it and configure
TypeScript — that's all.

## Package manager

<Tabs
  syncKey="pkg-manager"
  tabs={[
    {
      label: "npm",
      content: <CodeBlock code="npm install @vincle/core" language="bash" />,
    },
    {
      label: "bun",
      content: <CodeBlock code="bun add @vincle/core" language="bash" />,
    },
  ]}
/>

## TypeScript configuration

Set `jsxImportSource` in your `tsconfig.json` so that JSX files don't need any
explicit import:

<CodeExample src="guide/getting-started/tsconfig.json" meta="tsconfig.json" />

## Attribute types

Every HTML and SVG element is typed per element — `src` on `<img>`, `href` on
`<a>`. The types are part of `@vincle/core`: nothing to install, and a typo is
a compile error.

## What you get

### Runtime

| Export                            | Purpose                                             |
| --------------------------------- | --------------------------------------------------- |
| `renderToString(node)`            | Renders a JSX tree to an HTML string                |
| `raw(str)`                        | Marks a trusted HTML string (no escaping)           |
| `Fragment`                        | Standard JSX fragment (`<>…</>`)                    |
| `context`, `setContext`           | Scoped context for per-request isolation            |
| `useContext`, `withScope`         | Read a context value; run code in an isolated scope |
| `snapshot`                        | Captures current scope for child scopes             |
| `jsx-runtime` / `jsx-dev-runtime` | JSX transform entry points (auto-wired)             |

### Types

| Export          | Purpose                                                            |
| --------------- | ------------------------------------------------------------------ |
| `Renderable`    | Everything the renderers can render                                |
| `VNode`         | The engine's element representation — a class, exported as a value |
| `RawString`     | Branded type for pre-escaped HTML                                  |
| `Awaitable<T>`  | `T` or a promise of `T`                                            |
| `ClassValue`    | What a `class` attribute accepts                                   |
| `JSX`           | The JSX namespace                                                  |
| `ContextKey<T>` | Branded context key type                                           |
| `ContextMap`    | Raw context map type                                               |

Element attributes are typed per element, by the table generated into
`@vincle/core` — see
[JSX types](/api/core/jsx-types).

Everything else is internal. There is no plugin registry, no provider tree, no
config object to maintain.

## Where to next

- [First render](/guide/getting-started/first-render) — your first component
- [TypeScript setup](/guide/getting-started/typescript-setup) — Deno, esbuild
